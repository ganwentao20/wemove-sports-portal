# 开发环境变量（复制为 .env.local 使用；生产部署时改为真实网关地址）
# 说明：本地开发无需此文件 —— next.config.ts 已把 /api/v1/* 代理到 http://localhost:8080（apps/api）

# API 绝对地址（可选）：部署为独立域名/CDN 网关时使用，否则留空走同源代理
NEXT_PUBLIC_API_BASE_URL=

# API 代理目标（next.config.ts rewrites 用）
API_PROXY_TARGET=http://localhost:8080
